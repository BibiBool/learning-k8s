name: Flask Ci workflow
permissions:
  contents: read
  pull-requests: write

on:
  push:
    branches: [ "main" ]
    paths:
      - '02-backend-app-statefulset/api-flask/**'

jobs:
  linting-and-formatting:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./02-backend-app-statefulset/api-flask

    steps:
        - uses: actions/checkout@v5
        - name: setup python
          uses: actions/setup-python@v5
          with:
            python-version: "3.10.19"
        - name: Install Ruff
          run: pip install ruff
        - name: Lint code with Ruff
          run: ruff check .

  security-sast:
    needs: linting-and-formatting
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./02-backend-app-statefulset/api-flask
    steps:
      - uses: actions/checkout@v5
      - name: Run Bandit
        run: |
          pip install bandit
          bandit -r ./02-backend-app-statefulset/api-flask -ll
  
  unit-tests:
    needs: security-sast
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./02-backend-app-statefulset/api-flask
    steps:
      - uses: actions/checkout@v5
      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10.19"
          cache: 'pip'
      - name: install dependencies
        run: pip install -r requirements.txt 
      - name: run tests
        run: pytest
      - name: Upload HTML Coverage
        uses: actions/upload-artifact@v4
        with:
          name: html-coverage-report
          path: ./02-backend-app-statefulset/api-flask/htmlcov/

  build-image:
    needs: unit-tests
    runs-on: ubuntu-latest
    outputs: 
      sha_short: ${{ steps.vars.outputs.sha_short }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set short SHA
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Setup Docker Build
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: ./02-backend-app-statefulset/api-flask
          push: true
          tags: |
            bibiool/sanite:latest
            bibiool/sanite:v1.01
            bibiool/sanite:${{ steps.vars.outputs.sha_short }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
